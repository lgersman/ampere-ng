import Ampere from "../src/ampere";

describe("Ampere", function () {
  describe( "util", ()=>{
    it( "fooooo", done=>{
      /*
      let p = new Promise((resolve,reject)=>{
        resolve("hello world");
      });
      let result = Ampere.util.unPromisify(p);
      console.log( "test result = " + result);
      */
      done();
    });
  });
});
