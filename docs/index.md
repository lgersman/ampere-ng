# development

* Clone the GIT repository at Github : ``git clone https://github.com/lgersman/jquery.orangevolt-ampere2.git``

## installation

* install dependencies : ``npm install``

### commands

* call ``npm run`` and ``gulp --tasks`` to see a list of all available commands
